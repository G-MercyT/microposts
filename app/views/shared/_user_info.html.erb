<%= link_to gravatar_for(current_user, size: 50), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t('view.viewmyprofile'), current_user %></span>
<span><%= link_to t('view.followings'), followings_user_path(current_user) %>&nbsp;<%= link_to t('view.followers'), followers_user_path(current_user) %></span>
<span><%= pluralize(current_user.microposts.count, t('view.micropost')) %></span>