<%= render 'user_icon' %>
    <div class="col-md-8">
        <% if @user.follower_users.any? %>
            <h3>Followers (<%= @user.follower_users.count %>)</h3>
            <ol class="microposts">
                <%= render @follower %>
            </ol>
        <% end %>
    </div>
</div>
