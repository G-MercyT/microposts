<%= render 'user_icon' %>
    <div class="col-md-8">
        <% if @user.following_users.any? %>
            <h3>Followings (<%= @user.following_users.count %>)</h3>
            <ol class="microposts">
                <%= render @following %>
            </ol>
        <% end %>
    </div>
</div>
