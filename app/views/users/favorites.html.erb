<%= render 'user_icon' %>
<div class="col-md-8">
  <h2><%= t('view.favorites') %></h2>
  <div class="userlist">
    <ul>
      <% @favorites.each do |u| %>
        <%= render 'favorite', user: u %>
      <% end %>
    </ul>
  </div>
</div>
